name: syncitron
description: Enterprise local-first sync framework for Flutter with offline sync, conflict resolution, and pluggable backends.

version: 0.5.1
homepage: https://github.com/leonhardWullich/syncitron
repository: https://github.com/leonhardWullich/syncitron
documentation: https://github.com/leonhardWullich/syncitron/blob/main/docs_html/index.html
issue_tracker: https://github.com/leonhardWullich/syncitron/issues

environment:
  sdk: ^3.10.8
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter: