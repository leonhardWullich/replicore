name: flutter_local_first
description: Locl-first synchronization between Supabase and SQLite.
version: 0.0.1
homepage: https://github.com/leonhardWullich/flutter_local_first

environment:
  sdk: ^3.10.8
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.2
  supabase_flutter: ^2.12.0 
  shared_preferences: ^2.5.4
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: